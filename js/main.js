//begin with the jQuery method of checking to ensure page elements are ready
$(document).ready(function(){
  //initialize the carousel with an interval of 8000 milliseconds
  $('.carousel').carousel({
    interval: 8000
  });
});
